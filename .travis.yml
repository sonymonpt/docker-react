sudo: required
language: generic
services: 
    - docker

before_install:
    - docker build -t sonymonpt/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true sonymonpt/docker-react npm test
